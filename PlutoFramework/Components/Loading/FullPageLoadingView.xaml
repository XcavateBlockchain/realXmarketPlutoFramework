<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:animations="clr-namespace:PlutoFramework.Components.Animations"
             x:Class="PlutoFramework.Components.Loading.FullPageLoadingView"
             AbsoluteLayout.LayoutBounds="0.5, 0.5, 1, 1"
             AbsoluteLayout.LayoutFlags="All"
             IsVisible="{Binding IsVisible}">
    <Grid BackgroundColor="#F2FFFFFF">
        <animations:ParticleConvergenceView Playing="{Binding IsVisible}"
                                            ParticleColor="{StaticResource Primary}"
                                            ExclusionWidth="{Binding Source={x:Reference messageLabel}, Path=Width}"
                                            ExclusionHeight="{Binding Source={x:Reference messageLabel}, Path=Height}" />

        <Label x:Name="messageLabel"
               Text="{Binding Message}"
               TextColor="{StaticResource Primary}"
               HorizontalTextAlignment="Center"
               HorizontalOptions="Center"
               VerticalOptions="Center"
               Margin="40, 0" />
    </Grid>
</ContentView>
