<?xml version="1.0" encoding="utf-8" ?>
<template:PageTemplate xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                       xmlns:template="clr-namespace:PlutoFramework.Templates.PageTemplate"
                       xmlns:card="clr-namespace:PlutoFramework.Components.Card"
                       xmlns:buttons="clr-namespace:PlutoFramework.Components.Buttons"
                       x:Class="PlutoFramework.Components.Solana.Status.SolanaTransactionErrorPage"
                       Title="Transaction error">
    <ScrollView>
        <VerticalStackLayout Padding="20, 10, 20, 100"
                             Spacing="15">

            <card:Card>
                <card:Card.View>
                    <VerticalStackLayout Padding="15, 12, 15, 12"
                                         Spacing="4">
                        <Label Text="{Binding Description}"
                               FontAttributes="Bold"
                               FontSize="18" />
                        <Label Text="{Binding StatusText}"
                               FontSize="14"
                               TextColor="{Binding StatusColor}" />
                    </VerticalStackLayout>
                </card:Card.View>
            </card:Card>

            <card:Card IsVisible="{Binding HasErrorMessage}">
                <card:Card.View>
                    <VerticalStackLayout Padding="15, 12, 15, 12"
                                         Spacing="4">
                        <Label Text="Error"
                               FontSize="13"
                               TextColor="#A6A6A6" />
                        <Label Text="{Binding ErrorMessage}"
                               FontSize="14"
                               LineBreakMode="WordWrap" />
                    </VerticalStackLayout>
                </card:Card.View>
            </card:Card>

            <!-- Only when the transaction reached the network. A submission that threw has
                 nothing on the explorer to look at. -->
            <buttons:ElevatedButton Text="View on Solscan"
                                    ButtonState="Enabled"
                                    IsVisible="{Binding HasExplorerLink}"
                                    Clicked="OnExplorerClicked" />
        </VerticalStackLayout>
    </ScrollView>
</template:PageTemplate>
